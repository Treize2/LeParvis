fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2
beautifulsoup4==4.12.3
lxml==5.3.0
python-dateutil==2.9.0
ics==0.7.2
unidecode==1.3.8
# Optional headless browser, used only when an ingest request asks for
# JS rendering (e.g. messes.info loads its data via GWT-RPC client-side).
# The Chromium binary is installed by the Dockerfile, not by pip.
playwright==1.47.0
# In-process scheduler for the periodic refresh of imported parishes.
apscheduler==3.10.4
